version: 2

models:
  - name: sistemas_mais_usuarios
    description: "Tabela que mostra o tipo de navegador que os usuarios utilizam para acessar o sistema"
    config:
      tags: ["mart"]
    columns:
      - name: count_user
        description: "Contador que mostra quantidade de pessoas fazendo aquela acao"
        tests:
          - not_null